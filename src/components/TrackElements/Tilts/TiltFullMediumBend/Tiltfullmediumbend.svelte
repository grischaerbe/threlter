<!--
Auto-generated by: https://github.com/threlte/threlte/tree/main/packages/gltf
Command: npx @threlte/gltf@1.0.0-next.2 ./TiltFullMediumBend.glb -i -P -t -s -T
-->
<script context="module" lang="ts">
	import type * as THREE from 'three'
	import { Group } from 'three'
	import { T } from '@threlte/core'
	import { useGltf } from '@threlte/extras'
	import { AutoColliders } from '@threlte/rapier'
	import { useRefreshCollider } from '../../utils/useRefreshCollider'

	type GLTFResult = {
		nodes: {
			TiltFullMediumBend: THREE.Mesh
		}
		materials: {
			['Ramps.002']: THREE.MeshStandardMaterial
		}
	}

	const load = () => {
		return useGltf<GLTFResult>('/TiltFullMediumBend-transformed.glb', { useDraco: true })
	}

	export const preloadTiltfullmediumbend = async () => {
		await load()
	}
</script>

<script lang="ts">
	export const ref = new Group()

	const gltf = load()

	const { refreshFns } = useRefreshCollider()
</script>

{#if $gltf}
	<T is={ref} dispose={false}>
		<AutoColliders shape="trimesh" bind:refresh={refreshFns[0]}>
			<T.Mesh
				castShadow
				receiveShadow
				geometry={$gltf.nodes.TiltFullMediumBend.geometry}
				material={$gltf.materials['Ramps.002']}
			>
				<slot name="selection" />
			</T.Mesh>
		</AutoColliders>
		<slot {ref} />
	</T>
{/if}
