<!--
Auto-generated by: https://github.com/threlte/threlte/tree/main/packages/gltf
Command: npx @threlte/gltf@1.0.0-next.2 ./TiltTransitionRight.glb -i -P -t -s -T
-->
<script context="module" lang="ts">
	import type * as THREE from 'three'
	import { Group } from 'three'
	import { T } from '@threlte/core'
	import { useGltf } from '@threlte/extras'
	import { AutoColliders } from '@threlte/rapier'
	import { useRefreshCollider } from '../../../utils/useRefreshCollider'

	type GLTFResult = {
		nodes: {
			TiltTransitionRight: THREE.Mesh
		}
		materials: {
			Ramps: THREE.MeshStandardMaterial
		}
	}

	const load = () => {
		return useGltf<GLTFResult>('/TiltTransitionRight-transformed.glb', { useDraco: true })
	}

	export const preloadTilttransitionright = async () => {
		await load()
	}
</script>

<script lang="ts">
	export const ref = new Group()

	const gltf = load()
	const { refreshFns } = useRefreshCollider()
</script>

{#if $gltf}
	<T is={ref} dispose={false}>
		<AutoColliders shape="trimesh" bind:refresh={refreshFns[0]}>
			<T.Mesh
				castShadow
				receiveShadow
				geometry={$gltf.nodes.TiltTransitionRight.geometry}
				material={$gltf.materials.Ramps}
			>
				<slot name="selection" />
			</T.Mesh>
		</AutoColliders>
		<slot {ref} />
	</T>
{/if}
