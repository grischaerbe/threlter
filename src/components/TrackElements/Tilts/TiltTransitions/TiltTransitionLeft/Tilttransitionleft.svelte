<!--
Auto-generated by: https://github.com/threlte/threlte/tree/main/packages/gltf
Command: npx @threlte/gltf@1.0.0-next.2 ./TiltTransitionLeft.glb -i -P -t -s -T
-->
<script context="module" lang="ts">
	import type * as THREE from 'three'
	import { Group } from 'three'
	import { T } from '@threlte/core'
	import { useGltf } from '@threlte/extras'
	import { AutoColliders } from '@threlte/rapier'

	type GLTFResult = {
		nodes: {
			TiltTransitionLeft: THREE.Mesh
		}
		materials: {
			['Ramps.001']: THREE.MeshStandardMaterial
		}
	}

	const load = () => {
		return useGltf<GLTFResult>('/TiltTransitionLeft-transformed.glb', { useDraco: true })
	}

	export const preloadTilttransitionleft = async () => {
		await load()
	}
</script>

<script lang="ts">
	import { useRefreshCollider } from '../../../utils/useRefreshCollider'

	export const ref = new Group()

	const gltf = load()

	const { refreshFns } = useRefreshCollider()
</script>

{#if $gltf}
	<T is={ref} dispose={false}>
		<AutoColliders shape="trimesh" bind:refresh={refreshFns[0]}>
			<T.Mesh
				castShadow
				receiveShadow
				geometry={$gltf.nodes.TiltTransitionLeft.geometry}
				material={$gltf.materials['Ramps.001']}
			>
				<slot name="selection" />
			</T.Mesh>
		</AutoColliders>

		<slot {ref} />
	</T>
{/if}
