<!--
Auto-generated by: https://github.com/threlte/threlte/tree/main/packages/gltf
Command: npx @threlte/gltf@1.0.0-next.9 /Users/grischaerbe/Documents/Projects/2023/threltemania/src/components/TrackElements/Barrier/Barrier.glb -u -i -t -s
-->
<script lang="ts">
	import type * as THREE from 'three'
	import { Group } from 'three'
	import { T } from '@threlte/core'
	import { useGltf, useSuspense } from '@threlte/extras'
	import { Collider } from '@threlte/rapier'
	import { useRefreshCollider } from '../../utils/useRefreshCollider'

	export const ref = new Group()

	const suspend = useSuspense()

	type GLTFResult = {
		nodes: {
			Barrier: THREE.Mesh
		}
		materials: {}
	}

	const gltf = suspend(useGltf<GLTFResult>('/Barrier.glb'))

	const { refreshFns } = useRefreshCollider()
</script>

<T is={ref} dispose={false}>
	{#if $gltf}
		<T.Group position.y={0.3} position.z={-4.75}>
			<Collider shape="cuboid" args={[5, 0.3, 0.3]} type="static" bind:refresh={refreshFns[0]} />
		</T.Group>

		<T.Mesh
			castShadow
			receiveShadow
			geometry={$gltf.nodes.Barrier.geometry}
			material={$gltf.nodes.Barrier.material}
		>
			<slot name="selection" />
		</T.Mesh>
	{/if}

	<slot {ref} />
</T>
